{
  "name": "sendbird-ai-agent-samples",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "install:react": "cd js/react/sample && npm install",
    "install:cdn": "cd js/cdn/sample && npm install",
    "install:samples": "npm run install:react && npm run install:cdn",
    "dev:react": "cd js/react/sample && npm run dev",
    "dev:cdn": "cd js/cdn/sample && npm run dev",
    "build:react": "cd js/react/sample && npm run build",
    "build:cdn": "cd js/cdn/sample && npm run build",
    "build:samples": "npm run build:react && npm run build:cdn"
  }
}