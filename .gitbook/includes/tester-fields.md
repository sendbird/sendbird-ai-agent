---
title: Tester fields
---

<table><thead><tr><th width="194.4296875">Field</th><th>Description</th></tr></thead><tbody><tr><td>Agent</td><td>The AI agent you would like to test.</td></tr><tr><td>User ID</td><td>The Sendbird user ID. You can find users in <strong>Workspace settings > Users</strong>.<br>Different users may have unique attributes that can influence AI response.</td></tr><tr><td>Country</td><td><p>Specifies the test user's country. (Default: <strong>None</strong>)</p><ul><li><code>None</code>: Indicates no country is set for the user.</li><li>If no country is set, the AI agent will only use shared assets (e.g., Knowledge, Actionbooks) labeled "All countries."</li><li>If a country is set, (e.g., Korea), the AI agent will use shared assets labeled "All countries" and "Korea."</li></ul></td></tr><tr><td>Language</td><td><p>Select the language of the test user.<br>Your AI agent will respond using content in that language — if it exists.</p><ul><li>If a multi-language supported feature or Knowledge is available in the selected language, it will be used as-is.</li><li><p>If it's not available, the AI will:</p><ul><li>Use the default language version of the message to translate it to the user's selected language.</li></ul></li></ul></td></tr><tr><td>Context objects</td><td>Up to 5 key-value pairs that simulate additional metadata (e.g., membership = premium, device = iOS).</td></tr></tbody></table>
