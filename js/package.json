{
  "name": "sendbird-ai-agent-js-samples",
  "private": true,
  "version": "0.0.1",
  "description": "JavaScript samples and tests for Sendbird AI Agent",
  "scripts": {
    "install:react": "cd react/sample && npm install",
    "install:cdn": "cd cdn/sample && npm install",
    "install:samples": "npm run install:react && npm run install:cdn",
    "dev:react": "cd react/sample && npm run dev",
    "dev:cdn": "cd cdn/sample && npm run dev",
    "build:react": "cd react/sample && npm run build",
    "build:cdn": "cd cdn/sample && npm run build",
    "build:samples": "npm run build:react && npm run build:cdn",
    "test": "playwright test",
    "test:react": "cd react/sample && npm run test",
    "test:cdn": "cd cdn/sample && npm run test",
    "test:unit": "npm run test:react && npm run test:cdn",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}